shamefully-hoist=true
strict-peer-dependencies=false
# pinia-plugin-persistedstate@4.7.x 要求 @pinia/nuxt>=0.10，而本项目锁在 0.5.1，
# 不加这个选项 npm install/ci 会直接 ERESOLVE 失败（Vercel 构建同样受影响）
legacy-peer-deps=true
